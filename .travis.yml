sudo: false

language: php

php:
  - 7.2

branches:
  only:
    - master
    - "/^\\d+\\.\\d+\\.\\d+-?\\d+?$/"

script:
  - echo "NO PHPUNIT tests"

deploy:
  provider: releases
  api_key:
    secure: $GH_TOKEN
    file: "build.tar.gz"
    skip_cleanup: true
    draft: true